.panel.panel-default
  .panel-heading Disable two-factor authentication
  .panel-body
    .list-group
      li.list-group-item
        h3 Enter a 6-digit code the app generates to disable two-factor authentication
        = form_tag settings_disable_2fa_path, method: 'post', class: 'form-inline'
          - flash.each do |key, value|
            p.alert.alert-danger = value

          .form-group
            = text_field_tag :confirmation_otp, nil, class: 'form-control', placeholder: 'Authentication Code'

          button.btn.btn-danger Disable